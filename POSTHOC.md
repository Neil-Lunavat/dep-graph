# POSTHOC — changes made after results existed, and every bug fix

No main results exist yet. Only pre-results bug fixes are logged so far.

| Date | What changed | Why | Post-hoc or bug fix | What was re-run | Must the paper mention it? |
|---|---|---|---|---|---|
| 2026-09-22 | Line counting for rule R2: read files as UTF-8 first, then pygount's automatic detection; record pygount's per-file state | pygount's automatic mode fell back to the Windows code page (cp1252) on 879 UTF-8 files, logged a warning, and scored them as 0 lines instead of raising. The error counter read 0, so the failure was silent | Bug fix (before any sampling or results) | All 181 candidate measurements (`data/candidates.csv`); first run kept as `data/candidates_measure_run1.jsonl`. Effect: 5 repos' Python share moved by >0.01; one R2 outcome flipped (aio-libs/yarl 0.72 → 0.84, fail → pass) | No (pre-sampling tooling), but it is in the release log |
| 2026-09-22 | Docker Hub image check: per-image lookups at a steady 170/min instead of namespace listings | Docker Hub refuses anonymous listings past 100 items (HTTP 403) | Tooling fix | Image check restarted from cache | No |
| 2026-09-22 | Licence check (R5) also looks inside a top-level `LICENSE/` or `LICENSES/` folder | matplotlib keeps its licence in `LICENSE/LICENSE`; the root-only check marked it "no licence" | Bug fix (pre-sampling) | `results/step2/gate_a_*.csv`. Effect: matplotlib R5 fail → pass; repos passing all rules 115 → 116 | No |
| 2026-09-22 | Graph builder: skip directories whose names end in `.py`; log unreadable files instead of crashing; count every skipped `.py` file by reason | bridgecrewio/checkov has folders named `*.py`; the builder crashed on them. A code comment claimed skipped files were counted when they were not | Bug fix (Gate B prep; no graph used for results yet) | Cost-per-repo timing; builder comparison | No |
| 2026-09-22 | Graph builder: raise Python's recursion limit and log files that still overflow as parse failures; remove a stale git lock | One sympy commit crashed with RecursionError (deeply nested file); 21 pennylane commits failed to fetch because a killed run left `shallow.lock` behind | Bug fix (no results exist) | The 22 failed graphs; now 1,872 graphs, 0 failures | No |
| 2026-09-22 | Exec runner (before any key was used): force coverage>=5.5; drop the `-q` flag; neutralise repo-level pytest-cov (`--no-cov`); wrap test commands in `bash -c`; strip folder arguments from pytest commands; read coverage data directly instead of a JSON report (OOM); map Django docstring test names to functions; remove leftover containers; mark "no F2P lines" as its own status | Each found on the first live runs (old envs, pytest-cov repos, keras env-var command, matplotlib OOM, Django naming) | Bug fixes (execution key not yet used) | All execution runs restarted from scratch after the fixes | Limitations: code run in child processes (Django admin tests) has no test context |
| 2026-09-22 | Exec runner stopped after Docker Hub's 100-pulls/hour limit made 1,402 pulls fail; those results deleted for a later retry (runner should wait on rate limit or use an authenticated Docker Hub login) | Anonymous rate limit | Tooling issue | 129 runs kept; retry pending | Execution key covers a random subset only (D12 fallback) |
| 2026-09-22 | Method `embedding` not run (D23 compute: ~9 h on CPU); reported as pending on the findings page | Neil's 2-hour deadline | Scope, not a result-driven change | — | Yes: one inventory method missing |
| 2026-09-23 | Paper reframed from a 30-method bake-off on the union key to a structure-vs-issue-text comparison reported per evidence source | A ranking of methods dates quickly and the union key hid the reversal; the reframe was agreed with Neil before any new number existed | Post-hoc (framing), not a change to any measurement | New arm only: results/study2/. The original results/analysis/ numbers are untouched and still reproduce | Yes: the paper says the design of corpus, graphs, tasks and metric was frozen first and that the method families and analyses were added afterwards |
| 2026-09-23 | Retracted my own working hypothesis that the symbol key is more graph-recoverable than the co-edited key | The distance profile says otherwise: 86.7% of symbol files and 88.1% of co-edited files are within two hops. The keys are equally close; what differs is which ordering finds them first | Retraction of a claim I made in conversation before checking | results/study2/distance_profile.csv | No — it never entered a draft. Recorded so it is not re-invented |
| 2026-09-23 | Narrowed my earlier claim that 'a seed-independent PageRank gets 92% of the best method, so the seed barely matters' | It holds on the symbol key (0.410 vs 0.446) and fails on the co-edited key, where the same prior scores 0.272 against a random floor of 0.247 | Retraction of an over-general claim | results/study2/per_source.csv, pairwise.csv | Yes: RQ3 states the source-dependence explicitly |
| 2026-09-23 | Corrected the corpus counts quoted in the paper: the scored set is 2,412 tasks from 1,162 pull requests in 112 repositories, not 1,873 PRs in 113 repositories | I wrote the full-corpus figures next to the scored-task figure in the first draft of the new paper. 711 tasks have no key file inside the graph and are dropped, and that removes 711 PRs and one repository | Bug fix in the text; no measurement changed | Nothing re-run; `analysis2` now emits `n_*_scored` and a separate `corpus` block so the two cannot be confused again | No — caught before anyone read it, but the paper now states both numbers explicitly |
| 2026-09-24 | Added a token-denominated cost axis and a method-independent ceiling analysis (`src/depgraphs/{tokcost,ceiling}.py`) alongside the frozen line-based metric | A context window is measured in tokens, and coverage alone cannot say whether full coverage was possible at a budget | Addition, not a change: the pre-registered line metric and every number computed from it are untouched | New outputs only: `results/study2/{ceiling_key,ceiling_method,token_calibration,key_size}.csv` | Yes: §3.5 states that the budget stays in lines because freeze-1 fixed it, and reports the measured conversion (8.50 tokens per line) rather than assuming one |
| 2026-09-24 | Corrected the RepoGraph row of the prior-work table: it does report localization accuracy against the gold patch at file, function and line level; the draft said it had no localization key | I had written the row from a second-hand summary. Reading the paper's own evaluation section shows “the percentage of problems where the edit locations match the ground truth patch in three granularity” | Factual correction to a claim about another paper | `paper/paper.tex` Table 1. The correction strengthens the argument: one more system uses the patch key rather than abstaining | No — corrected before the draft left the machine, but recorded because the error class matters |
| 2026-09-24 | Moved GraphLocator out of the patch-key block: it extracts locations from the fix patch but then dual-annotates and cross-reviews them, explicitly because patch-derived ground truth “contains missing locations” | Same cause — the row was written from a summary that omitted the human-verification step | Factual correction | `paper/paper.tex` Table 1 | No |
| 2026-09-24 | Rewrote the related-work positioning after reading three concurrent 2026 benchmarks in full. SWE-Explore grades against regions successful agent trajectories actually consulted, under a fixed line budget; ContextBench uses human-annotated gold context; AgentRetrievalBench defines relevance as what helps the agent's next step, under a top-k or token budget | The first draft implied the field had not questioned the patch key. At least four efforts did so during this study, and two use a budget-based cost model close to ours | Correction of a novelty claim | `paper/paper.tex` §2. The claim is now narrower and defensible: they propose better keys, we measure that the choice of key reverses the ranking of methods | Yes: the paragraph names each system and states what it does that we do not |
| 2026-09-24 | Added directed personalised PageRank in both edge directions, cost-scaled and not (`ppr_out`, `ppr_out_pl`, `ppr_in`, `ppr_in_pl`), and re-ran the whole study | An external review noted that every walk we tested was undirected, and that the directed form is the obvious variant for a dependency-style key | Addition of methods after the freeze; exploratory, like the fusion family | All of `results/study2/`. Ranks now run over 23 orderings, 20 contenders. New finding: `ppr_out_pl` is 2nd on the symbol key (0.491) and `ppr_in` 22nd (0.269), while every directed variant is 17th-20th on co-edited, below `same_dir` | Yes: §4.3 is a new subsection, and §3.1 now states that the freeze covers corpus, graphs, tasks and metric but not the method families |
| 2026-09-24 | **Narrowed the paper's co-edited headline claim.** Adding a stricter leakage control — dropping tasks whose issue names a key file by bare *stem*, not only by full path — moves `path_issue` from 5th to 13th on the co-edited key (0.434 → 0.336) and lifts `hops_lines` to 5th, ahead of every pure lexical method | The review pointed out that the full-path control was too weak because path BM25 matches on stems. It was right: 48.5% of tasks contain a key-file stem | Result-driven narrowing of a claim | `results/study2/leak_sensitivity.csv` now carries a `stratum` column. The claim “words beat structure on co-edited files” now holds on the full corpus and is stated as conditional; what survives both controls is that the key decides which family wins, and that fusion holds the top four co-edited places | Yes: §4.9 reports both strata and says explicitly what we no longer conclude |
| 2026-09-24 | Corrected six claims that disagreed with our own tables: “every fusion beats every pure signal” (3 of 5 do), “`rrf_hops_path` third or better in all eight size cells” (worst is 5th), “three of the top four are structural” under the symbol key (two are), the best method's full-coverage share on the symbol key (`hops_lines` 51.0%, not 50.4%), “tenth of sixteen” (tenth of 23, ninth of 20), and “widens with size” (the rank gap is not monotonic in any method) | Every number in the paper was copied mechanically from a CSV; every sentence *about* those numbers was written by hand and nothing checked them | Factual corrections | `paper/paper.tex`. Systemic fix: `src/depgraphs/maketables.py` now generates every numeric table row from the CSVs, so no row is hand-transcribed, and `src/depgraphs/checknums.py` recomputes each prose-level claim | No — the errors were caught in review before submission |
| 2026-09-24 | Demoted the Spearman rank-agreement statistic from a headline to a secondary observation | With 20 methods a non-significant ρ is weak evidence of no relationship, and its value moves with which methods are included — adding the four directed walks changed it from 0.26 to 0.24 (contenders) and 0.37 (all) | Correction of an over-claim | Abstract, Contribution 2, §2 and §4.2 now rest the reversal on the Holm-corrected pairwise tests | Yes |
| 2026-09-24 | Conceded in the abstract that the symbol key is partly circular with the methods it favours | The key is built with `jedi`'s `goto(follow_imports=True)`, which traverses import edges, so import-graph walks winning against it is close to definitional | Framing correction | §3.3 now names the analyser and states the bound; the abstract says the co-edited result is the one that carries the paper | Yes |
| 2026-09-24 | **Withdrew the symbol-key half of the headline reversal.** The two keys were scored on different populations: a co-edited key needs a multi-file pull request, a symbol key does not, and 444 of the 1{,}156 symbol pull requests change a single file. Restricted to the 702 pull requests carrying both keys, `ppr_out_pl` falls from 2nd to 8th on the symbol key and no longer beats `path_issue` (p_Holm = 1, was < 10^-8) | An external review identified the population confound and called the fix essential. It was right | Result-driven withdrawal of a claim | New `per_source_shared.csv`, new `shared_prs()` and `table_shared()` in `analysis2.py`, new §4.3 and Table 6 | Yes — the abstract, contributions and conclusion now state that the effect does not survive population matching |
| 2026-09-24 | Switched all paired tests from the pull request to the **repository** as the unit | Pull requests cluster within 112 repositories; treating them as independent is what produced p < 10^-100 from ~1{,}100 pull requests | Correction of an inflated statistic | `pairwise()` takes a `unit` argument; `pairwise.csv` now carries `unit` and `population` columns and holds all four views. Effect sizes are unchanged; the largest p-value in the paper moved from 10^-100 to 10^-20, and two comparisons lost significance entirely | Yes — §3.6 states the unit and the reason |
| 2026-09-24 | Split issue-text leakage into **explicit** and **incidental** mentions and made the explicit-only stratum primary | The review argued the bare-stem control over-corrects: an issue about “models” and a key file `models.py` share a word because a project's prose and its filenames share a vocabulary, which is signal, not leakage | Refinement of a control | New `src/depgraphs/leakage.py` (the old `issue_path_leak.csv` had **no generator in the repository** — it came from a scratch script; the reimplementation reproduces the published strata exactly). Explicit mentions cover 40.2% of tasks. `path_issue` falls to 11th under the primary control and 13th under the strictest, so the finding does not depend on over-correcting | Yes — §4.9 reports three nested strata |
| 2026-09-24 | Corrected nine further claims that disagreed with our tables: Contribution 6 said every directed *and undirected* variant falls below `same_dir` on co-edited (only the directed ones do); Contribution 4 and the Table 7 caption attributed the fusion split to path matching (four of five fusions include it — the real split is cost-scaling of the structural input); the Table 7 caption said “twenty-two non-oracle orderings” while printing oracle-inclusive ranks; the abstract compared a rank on the 23-ordering scale with one on the 20-contender scale; the abstract paired `ppr_out_pl` with the non-cost-scaled `ppr_in`; §4.9 said `path_issue` was 10th on symbol in Q4 (12th) and `rrf_hops_path` third-or-better in six size cells (five); §4.7 said the structural orderings gain moving to the symbol key (both inward walks lose ~11 points); Table 2 omitted the four directed walks; Table 5 was from an earlier run | A second external review of the same PDF | Factual corrections | `paper/paper.tex`; Table 5 is now generated by `maketables.py`, so **no numeric row in the paper is hand-transcribed** | No — caught in review before submission |
| 2026-09-24 | Fixed a string-escape bug that printed `p_\text{Holm}` as `p_extHolm` in two places | `\t` in a non-raw Python string became a tab when an edit script wrote the LaTeX | Typographical correction | `paper/paper.tex` §4.9. Also repaired two table row terminators that a shell heredoc had collapsed from `\\` to `\` | No |
| 2026-09-24 | Rewrote the title, §5, §6, §7 and §8 around what survives the two controls | The review noted that the new results had been written into §4 and the abstract but not carried through, leaving several passages contradicting the paper's own findings — Threats still said leakage was “shown not to drive the result” while §4.9 said it does | Internal consistency | Title is now *What Belongs in an Agent's Context Window*. The thesis is fusion robustness plus the two controls, not the reversal. §6 pre-registers the best structural ordering per key rather than naming one | Yes |
| 2026-09-24 | Softened the novelty and RQ6 claims | The review pointed out that SWE-bench's own sparse baseline is BM25 over the issue text and that the agentic systems read the issue, so “a lexical signal belongs in every graph tool” partly describes existing practice; and that an oracle reading only key files fits a small key into a small budget almost by construction | Correction of two over-claims | §1 now credits the prior lexical baselines; §4.6 states the ceiling as an upper bound rather than headroom. A claim that Aider personalises its ranking toward chat-mentioned identifiers was **dropped**: the cited blog post does not say so and we could not verify it | Yes |
| 2026-09-24 | **Added an issue-ablation experiment and made it the paper's main result.** For each leading fusion we built an *issue-free twin*: the identical ordering with every issue-derived input replaced by its seed-derived counterpart (same walk, same number of lists, same BM25, no issue). The difference is what the issue contributes. It is $+0.156$ AUC on the tasks whose issue names a key file as code and $+0.016$ (n.s.) on the rest, falling to $+0.003$ when the stem must be absent too --- monotone across five strata, both pairs, both keys | Three external reviews all named the “a fusion just gets more input” confound as the thing blocking the central recommendation, and all three said the experiment was cheap. It was | New analysis answering a pre-existing objection | Two methods added to `study2.py` (`rrf_hops_pathseed`, `rrf_pprpl_seedpath`), appended after `random` so the RNG stream of every pre-existing method is untouched --- the rebuild reproduces all 154{,}353 prior rows bit-identically. New `tab_issue.tex`, new §4.10 | Yes — the title, abstract, contributions, discussion and conclusion now lead with it |
| 2026-09-24 | **Applied the two controls jointly**, recomputing the matched population inside each leakage stratum | All three reviews observed that the population control was applied mainly to the symbol key and the leakage control mainly to the co-edited key, never together, so “survives every control” was untested | Strengthening a control | New `table_joint()` and `per_source_joint.csv`; Table 11 is now the jointly-controlled table. The result is mixed and reported as such: the fusion leads on co-edited files in every stratum but sits behind two length-aware walks on symbol files. **“Survives every control” was withdrawn** and replaced by “never significantly worse than the best pure ordering under any control” | Yes |
| 2026-09-24 | **Ran a held-out repository split.** A random 56 of the 112 repositories select the fusion by worst-case rank; the other 56 report it | The fusion recipe and the selection criterion were both chosen after seeing the results, making the recommendation exploratory. Every review asked for this | Confirmation of an exploratory choice | New `heldout()` and `tab_heldout.tex`, new §4.6. The criterion picks `rrf_pprpl_issue_path` on the selection half, and it is the best non-oracle ordering on the held-out half under both keys (0.492 / 0.469) | Yes |
| 2026-09-24 | Added **paired tests to every leakage-stratum rank change** | Two reviews noted that Table 11 reported rank movements with no p-values while the rest of the paper corrected everything. The tests do not all support the prose: `hops_lines` over `path_issue` on co-edited files is $+0.042$ with $p_{Holm} = 0.40$ in the primary stratum and only reaches significance in the strictest ($+0.056$, $p = 0.02$) | Correction of an untested claim | `pairwise()` takes `tasks` and `stratum`; `pairwise.csv` now carries the stratified tests. **“Path matching loses to pure structure” was softened to “becomes indistinguishable from”**, and the reduced power of the strata is stated | Yes |
| 2026-09-24 | Corrected the incidental-mention rate from 11.7\% to **8.3\%** | All three reviews independently noticed the strata did not add up. The 11.7\% counted every task with an incidental mention, including tasks that also had an explicit one, so explicit + incidental exceeded the share with any stem. The disjoint figure is 199/2{,}412 | Arithmetic correction | `leakage.py` now prints the disjoint share and `checknums.py` asserts explicit + incidental-only = stem | No — caught before submission |
| 2026-09-24 | Reported the **454 single-file pull requests as a finding**, not an artefact | A review argued that calling the population mismatch a sampling artefact undersells it: single-file changes are real work. On those 454, `ppr_out_pl` scores 0.605 and the six best orderings are all structural, against 0.417 for the same walk on the matched population | Reframing, with new numbers | §5. Structure's advantage on the symbol key is *concentrated* in single-file changes, which is intuitive and worth stating directly | Yes |
| 2026-09-24 | Fixed a third instance of the **string-escape class of bug**: `\ref` written in a non-raw Python string became a carriage return, printing “Table eftab:main” and “Sections efsec:shared and efsec:leak” in §4.11 | Same cause as the earlier `\t` and `\\` incidents: a patch script built LaTeX in a non-raw string | Typographical correction | `paper/paper.tex`. The standing rule is now to write patch scripts to a file rather than through a shell heredoc, and to build every backslash with `chr(92)` | No |
| 2026-09-24 | Unified the **rank scale** to the twenty-three orderings, oracle included, everywhere | Reviews reported ranks switching between 23, 22 and 20 across tables and prose, and Table 11 had used the twenty-contender scale while Table 5 used the twenty-three. The two fusion controls are scored but deliberately **not ranked**, since ranking them would have shifted every published rank by up to two places | Presentation correction | `auc_table()` excludes `FUSION_CTL` from ranking and from the rank correlation; verified that all 69 published per-source ranks and AUCs are unchanged | No |
| 2026-09-24 | Stated that **quoted differences are repository-level** while table AUCs are pull-request-level | Every review flagged the same apparent errors (abstract 0.055 against Table 4's 0.052, §4.7's $+0.003$ against $-0.001$). The text was right and the unit was unstated | Presentation correction | §4.2. Both figures are now labelled at first use | No |
| 2026-09-24 | Added **`src/depgraphs/claims.py`**, which recomputes every number quoted in the paper and asserts that the manuscript contains it | A reviewer's suggestion: “better still, have the scripts emit those sentences”. Three cycles of this project have been spent on numbers that were correct when written and stale when the analysis under them changed | New verification tooling | 36 claims checked, 0 disagreements. Registered as a `run.py` stage after `maketables`. It found two real errors on first run: a 0.047 that should have been 0.049, and a delta quoted on the wrong population | No |
| 2026-09-24 | Corrected two bibliographic items and **declined a third** | CoSIL is now published at ASE 2025 rather than “to appear”. SWE-Bench+ (Aleithan et al., 2024) measures *solution* leakage --- 32.67\% of SWE-Agent's passed patches had the fix already in the issue --- and is now cited as the closest prior work to our control. A reviewer also suggested Agentless classifies issues by how much *location* information they give; checking the paper, it classifies by whether the ground-truth patch appears and by description adequacy, **not** by location granularity, so that claim was not made | Verification | `references.bib`, §2. This is the second claim in two cycles that a review offered and the source did not support | Yes — §1's novelty claim is narrowed accordingly |
| 2026-09-24 | Repaired §6, the pre-registered downstream protocol | A review pointed out that condition (ii) was “defined per key”, which is meaningless in a repair experiment where the model gets one context and the tests either pass or fail; that the protocol still said “pull request as the unit as elsewhere in this paper” after we had moved to repositories; and that it pre-registered a different fusion from the one the abstract recommends | Correction of an inconsistent pre-registration | §6 now has seven conditions, each naming one concrete ordering, carries **both** candidate fusions plus the issue-free twin, and specifies a mixed-effects model with a random intercept per repository | Yes |
| 2026-09-24 | **Ran the within-task redaction experiment, and it corrected our own headline.** For every task we take the same issue and delete only the explicit mentions of its key files' names, leaving the prose, the traceback, the directory names and the length intact, then score the task on both versions | A review pointed out that stratifying tasks cannot show *why* the issue helps: issues that name files are plausibly longer and more technical, so the stratum comparison is confounded with issue quality. It was right | New experiment answering a valid objection to our main claim | New `src/depgraphs/redact.py`, a redaction arm in `study2.py`, `redaction()` in `analysis2.py`, new Table 14. Deleting the names alone costs `path_issue` 0.110 AUC and the best fusion 0.061; on the 1{,}442 tasks where the redactor removed nothing the difference is at most 0.002 in all eight cells | Yes — the claim changed from *the issue is worth what it names* to a decomposition |
| 2026-09-24 | **Withdrew “almost all of the value of issue text is the issue naming the answer”** and replaced it with an additive decomposition | The redaction experiment shows it overstated. On the tasks whose issue names a key file the issue as a whole is worth $+0.156$ AUC, of which the names are $+0.061$ and the rest of the issue $+0.095$; on tasks naming nothing the whole issue is worth $+0.016$. So reports that name files are *also* better reports, and leakage is about two fifths of the gap, not all of it | Result-driven correction of an over-claim | §4.10, abstract, contribution 7, discussion, conclusion. `checknums.py` asserts the decomposition is additive (residual 0.0000) | Yes |
| 2026-09-24 | Replaced the single held-out split with **200 random repository splits** | A review noted that one 56/56 split choosing among five fusions, when the winner was already best overall, is close to a foregone conclusion | Strengthening a confirmation | `rrf_pprpl_issue_path` is selected in 198/200 splits and is the best non-oracle ordering on the half that did not select it in 195 (co-edited) and 198 (symbol), mean shortfall 0.002 and 0.000 | Yes |
| 2026-09-24 | Added a **worst-case AUC shortfall** criterion beside worst-case rank | A review observed that rank-based robustness depends on the competitor set — adding our four directed walks reshuffled everything below them — so a criterion that moves when other people add methods is a poor one to recommend | Methodological improvement | `rank_by_source.csv` gains `worst_gap`; Table 8 sorts by it. It resolves an inconsistency the reviews kept flagging: the shortfall criterion, the matched population and the held-out splits all select `rrf_pprpl_issue_path`, and only the rank criterion preferred `rrf_hops_path` | Yes — one recommended method throughout |
| 2026-09-24 | Added **confidence intervals to the null results** | A review noted we used $+0.016$ rhetorically as if it were zero (“removes nine tenths”, “an order of magnitude”) while admitting the strata are underpowered | Correction of rhetoric | §4.10 now reports $[0.000, 0.031]$ and $[-0.003, 0.027]$ and says the data are consistent with the issue being worth up to about 0.03 there, not that it is worth nothing | Yes |
| 2026-09-24 | Checked **RRF's $k$**, the one free parameter we never varied | A review asked for it as an obvious reviewer question | Sensitivity check | Re-ran the whole pipeline at $k = 10$ and $k = 200$ against the customary 60: the headline fusion's AUC moves by at most 0.007 and 0.010, an order of magnitude below the differences the paper turns on | Yes — one sentence in §4.6 |
| 2026-09-24 | Stopped calling the design freeze a **pre-registration** | A review pointed out that a signed tag in our own version control is not an external registration, and that the method families, the fusion approach, the worst-rank criterion and RQ7 were all added afterwards | Correction of an over-claim about our own rigour | §3.1 and the §6 heading. We now say the freeze establishes the order in which we did things to a reader who trusts the repository and to nobody else | Yes |
| 2026-09-24 | Errata from the same review, all confirmed against the tables: Threats still said no held-out split existed while §4.6 performs one; the single-file count was 444 in §4.3 and 454 elsewhere (454 is right, $1{,}156 - 702$); contribution 8 quoted the retired twenty-contender rank scale; the Table 13 caption called the first row the complement of the third when it is the fourth; the $p$-value moving from $10^{-100}$ to $10^{-20}$ was called the largest when it is the smallest; §4.9 said two walks were “first and second” on an oracle-inclusive scale where they are second and third; §4.5 called `ppr_und_pl` (0.489) the best length-aware walk when `ppr_out_pl` scores 0.491; the median-key and ceiling figures looked contradictory and are now reconciled explicitly (task median 1{,}150 lines against a pull-request median of 991, about 7{,}800 tokens) | External review | Factual corrections | `paper/paper.tex`; the reconciliation and the size-quartile ranks were added to `claims.py` | No — caught before submission |
| 2026-09-24 | Stated the **answer-key limitation** plainly in Threats | A review noted that the co-edited key conflates files that were changed with files that had to be read, and that concurrent work builds keys from human annotation and agent trajectories | Honesty about construct validity | §7 now names scoring a subset against such a key as the single addition we would most want to make | Yes |
| 2026-09-24 | Fixed a **circular pipeline dependency** introduced by the redaction stage | `redact.py` filtered to tasks scored by `study2`, but `study2` now consumes `redact`'s output, so a clean `run.py all` could not have run | Reproducibility bug, found by registering the stage rather than by a review | `redact.py` processes every task in `tasks.jsonl`; stage order is now `issues, redact, study2` | No |
